[project]
name = "draftcode-github-com"
version = "0.1.0"
description = ""
authors = [{ name = "Masaya Suzuki", email = "draftcode@gmail.com" }]
dependencies = ["mkdocs-material", "markdown-cjk-spacing"]
requires-python = ">= 3.13"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["src/draftcode_github_com"]
